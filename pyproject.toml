[project]
name = "thermography"
version = "0.1.0"
description = "Professional thermography website built with Django + Wagtail CMS"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "python-dotenv>=1.2.1",
    "wagtail>=7.3",
]
