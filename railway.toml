[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
# Temporarily disabled healthcheck to debug startup
# healthcheckPath = "/"
# healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
startCommand = "bash start.sh"
