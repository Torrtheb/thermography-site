# Thermography Website — Project Status

## Overview
A professional website for a thermography (health screening) business, built with Django + Wagtail CMS. The business owner is non-technical and needs to manage all content, availability, and pricing from an intuitive admin panel.

## Key Requirements
- **Free hosting** (no paid plans) — Google Cloud Run free tier
- **Professional & beautiful** interface with scroll animations (AOS)
- **Non-technical owner** must manage everything from Wagtail admin (simpler than WordPress)
- **No client login** — clients book by selecting a slot and entering contact info
- **Secure** — Django built-in security (CSRF, XSS, SQL injection protection)
- **Modular code** — each feature is a separate Django app
- **Python backend** — developer is learning Django/web dev, knows Python basics
- **Package manager**: `uv` (not pip)
- **Python version**: 3.13 (pinned via uv)
- **Future**: AI booking agent (LangGraph), payment integration (Stripe)

## Tech Stack
- **Framework**: Django 6.0 + Wagtail 7.3
- **Styling**: Tailwind CSS (CDN) + AOS scroll animations
- **Database**: SQLite (dev) → Postgres (production)
- **Package manager**: uv
- **Deployment target**: Google Cloud Run (free tier)
- **Repo**: https://github.com/Torrtheb/thermography-site

## Website Pages
| Page | Django App | Description |
|---|---|---|
| Welcome / Home | `home` | Hero, about, testimonials, news, CTA — StreamField (admin-editable) |
| Services | `services` | Service cards with descriptions, duration, pricing |
| Who We Are | `about` | About us + team members |
| Resources | `blog` | Articles, blog posts, external article links |
| FAQ | `faq` | Collapsible Q&A accordion |
| Contact | `contact` | Contact form with email notification |
| Book Appointment | `booking` | Calendar, slot picker, booking form (no login), confirmation |

## Project Structure
```
thermography/
├── manage.py                          # Django CLI
├── pyproject.toml                     # uv dependencies
├── uv.lock                           # lockfile
├── db.sqlite3                         # local database
├── .env.example                       # env var template
├── .gitignore
├── Dockerfile                         # generated by Wagtail (customize later)
├── requirements.txt                   # generated by Wagtail (uv manages deps)
│
├── thermography_site/                 # project config
│   ├── settings/
│   │   ├── base.py                    # shared settings, INSTALLED_APPS
│   │   ├── dev.py                     # DEBUG=True, SQLite
│   │   └── production.py             # DEBUG=False, Postgres
│   ├── urls.py                        # URL routing
│   ├── wsgi.py
│   ├── static/
│   │   ├── css/thermography_site.css
│   │   └── js/thermography_site.js
│   └── templates/
│       ├── base.html                  # master layout (navbar + footer + Tailwind + AOS)
│       ├── 404.html
│       ├── 500.html
│       └── includes/
│           ├── navbar.html            # sticky navbar + slide-in mobile sidebar
│           └── footer.html            # 3-column footer
│
├── home/                              # Homepage app
│   ├── models.py                      # HomePage with StreamField body
│   ├── blocks.py                      # Block types: Hero, TextWithImage, Testimonial, News, CTA
│   └── templates/home/
│       ├── home_page.html             # renders StreamField blocks
│       └── blocks/                    # individual block templates
│           ├── hero_block.html
│           ├── richtext_block.html
│           ├── text_with_image_block.html
│           ├── testimonial_block.html
│           ├── news_item_block.html
│           └── cta_block.html
│
├── services/                          # Services app (Phase 5)
│   ├── models.py
│   ├── views.py
│   └── templates/services/
│
├── about/                             # Who We Are app (Phase 5+)
│   ├── models.py
│   ├── views.py
│   └── templates/about/
│
├── blog/                              # Resources app (Phase 6)
│   ├── models.py
│   ├── views.py
│   └── templates/blog/
│
├── booking/                           # Booking app (Phase 9)
│   ├── models.py
│   ├── views.py
│   └── templates/booking/
│
├── contact/                           # Contact app (Phase 8)
│   ├── models.py
│   ├── views.py
│   └── templates/contact/
│
├── faq/                               # FAQ app (Phase 8)
│   ├── models.py
│   ├── views.py
│   └── templates/faq/
│
└── search/                            # Search app (Wagtail generated)
    ├── views.py
    └── templates/search/
```

## Build Phases & Status

### Phase 1: Project Setup ✅ COMPLETE
- uv init, wagtail installed, project scaffolded
- SQLite database, migrations run, superuser created
- Git repo initialized, pushed to GitHub

### Phase 2: Project Structure ✅ COMPLETE
- Created all 6 Django apps: services, blog, about, booking, contact, faq
- Registered in INSTALLED_APPS
- Template directories created for each app

### Phase 3: Base Template & Styling ✅ COMPLETE
- base.html: Tailwind CDN, AOS animations, template blocks
- navbar.html: sticky nav, desktop links, slide-in mobile sidebar with backdrop
- footer.html: 3-column dark footer with dynamic contact info from ContactPage
- Brand colors: blue palette — calm, healthcare-appropriate
- Home link removed from nav (logo goes to home)
- Breakpoint changed to lg for hamburger menu
- Accessibility: aria-labels on mobile buttons, correct heading hierarchy

### Phase 4: Homepage (StreamField) ✅ COMPLETE
- home/blocks.py: 5 block types (Hero, TextWithImage, Testimonial, NewsItem, CTA)
- home/models.py: StreamField body
- 6 block templates: hero, richtext, text_with_image, testimonial, news_item, cta
- Tailwind Typography plugin loaded via CDN
- home_page.html renders StreamField blocks

### Phase 5: Services Page ✅ COMPLETE
- ServicesIndexPage + ServicePage models
- Service cards grid with image, price, duration, "Learn More" link
- Detail page with rich description, summary box, "Book This Service" CTA

### Phase 6: Blog / Resources ✅ COMPLETE
- BlogIndexPage + BlogPage models
- Article cards grid with cover image, date, author, excerpt
- External article support (external_url field, "External ↗" badge, opens in new tab)
- Detail page with cover image, summary, rich text body

### Phase 7: About Page ✅ COMPLETE
- TechnicianPage model (full_name, headshot, bio, credentials, years_experience)
- Profile layout: photo + bio side by side, credentials section, booking CTA
- Renamed "Who We Are" → "About" in nav and footer

### Phase 8: FAQs & Contact ✅ COMPLETE
- FAQPage model with StreamField of FAQItemBlocks
- Collapsible accordion with JavaScript toggle
- ContactPage model (email, phone, address, map embed, form toggle)
- Two-column layout: contact info + form
- Footer contact info dynamically pulled from ContactPage via template tag

### Phase 9: Booking Page ✅ COMPLETE
- BookingPage model (headline, instructions, embed URL, direct link, button text)
- Three display modes: iframe embed / direct link button / "Coming Soon" fallback
- Designed for Cal.com integration (free external booking service)

### Phase 10: Polish & Cleanup 🔧 IN PROGRESS
- ✅ Deleted old welcome_page.html and stray .DS_Store
- ✅ Fixed WAGTAIL_SITE_NAME and WAGTAILADMIN_BASE_URL
- ✅ Updated .env.example with all needed variables
- ✅ Updated pyproject.toml description
- ⬜ Wire up contact form to send email
- ⬜ Security hardening (production settings)

### Phase 11: Deployment (Cloud Run) ⬜ NOT STARTED
### Phase 12: AI Agent (Future) ⬜ NOT STARTED

## Design Decisions
- **Tailwind via CDN** (no build tools) — switch to compiled for production later
- **SQLite for dev** — switch to Postgres for production (one settings change)
- **StreamField for homepage** — owner can add/remove/reorder sections freely
- **Slide-in sidebar** on mobile (not dropdown) — main content stays visible
- **Blue brand palette** — calm, healthcare-appropriate
- **No client login** — booking is name/email/phone only
- **lg breakpoint** for mobile nav — prevents link text wrapping

## Common Commands
```bash
uv run python manage.py runserver          # start dev server
uv run python manage.py makemigrations     # generate migration after model change
uv run python manage.py migrate            # apply migrations to database
uv run python manage.py createsuperuser    # create admin user
uv add <package>                           # install a package
```
